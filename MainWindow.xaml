<Window x:Class="EasyResampWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EasyResamp Pro" Height="600" Width="500"
        Background="#1E1E1E" Foreground="White"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True" Drop="Window_Drop"
        Closing="Window_Closing">

    <Window.Resources>
        <Style x:Key="ModernScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
            <Setter Property="Foreground" Value="#444" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Width="10" Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" Focusable="false">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb" Background="{TemplateBinding Foreground}">
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border Background="{TemplateBinding Background}" CornerRadius="5"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Thumb" Property="Background" Value="#666" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter />
                            <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="1" 
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Style="{StaticResource ModernScrollBar}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="USTAWIENIA" FontWeight="Bold" Foreground="#007ACC" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <TextBlock Text="Szerokość:" VerticalAlignment="Center" Width="70"/>
                <TextBox x:Name="txtWidth" Text="1920" Width="60" TextAlignment="Center"/>
                <TextBlock Text="px" VerticalAlignment="Center" Margin="5,0,15,0" Foreground="Gray"/>

                <TextBlock Text="Wysokość:" VerticalAlignment="Center" Width="70"/>
                <TextBox x:Name="txtHeight" Text="1080" Width="60" TextAlignment="Center"/>
                <TextBlock Text="px" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="Gray"/>
            </StackPanel>

            <StackPanel Margin="0,0,0,5">
                <RadioButton x:Name="rbAsk" Content="Pytaj o folder za każdym razem" IsChecked="True" 
                             Foreground="White" GroupName="FolderOpt" Margin="0,0,0,5" Checked="FolderOption_Changed"/>

                <StackPanel Orientation="Horizontal">
                    <RadioButton x:Name="rbFixed" Content="Stały folder:" 
                                 Foreground="White" GroupName="FolderOpt" VerticalAlignment="Center" Checked="FolderOption_Changed"/>
                    <TextBox x:Name="txtFixedPath" Width="200" Margin="10,0,5,0" IsReadOnly="True" Opacity="0.5"/>
                    <Button Content="..." Width="30" Click="BtnBrowseFolder_Click"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#3E3E42" BorderThickness="2" CornerRadius="10" 
                Margin="0,20,0,10" Padding="20" Background="Transparent" AllowDrop="True"
                Cursor="Hand" MouseLeftButtonDown="BtnAddFiles_Click">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="⬇ Przeciągnij zdjęcia tutaj ⬇" FontSize="16" FontWeight="Bold" 
                           HorizontalAlignment="Center" Foreground="White"/>
                <TextBlock Text="lub kliknij w ten obszar, aby wybrać pliki" FontSize="12" 
                           HorizontalAlignment="Center" Foreground="White" Margin="0,5,0,0" Opacity="0.8"/>
            </StackPanel>
        </Border>

        <ListBox Grid.Row="2" x:Name="lstFiles" Background="Transparent" BorderThickness="0" 
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>

                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#252525"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#2A2A2A"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#333" BorderThickness="0,0,0,1" Padding="5,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding NazwaPliku}" Foreground="#DDD" 
                                       VerticalAlignment="Center" FontSize="13"
                                       TextTrimming="CharacterEllipsis"/>

                            <Button Grid.Column="1" Content="✕" Background="Transparent" Foreground="#E74C3C" 
                                    FontSize="14" FontWeight="Bold" Margin="10,0,0,0"
                                    Click="BtnRemoveItem_Click" Tag="{Binding SciezkaPelna}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Margin="0,15,0,0">
            <ProgressBar x:Name="progressBar" Height="5" Margin="0,0,0,10" Background="#3E3E42" Foreground="#1E7E34"/>
            <Button Content="ROZPOCZNIJ PRZETWARZANIE" Height="50" FontSize="15" FontWeight="Black" 
                    Background="#1E7E34" Foreground="White" Click="BtnStart_Click"/>
            <TextBlock x:Name="lblStatus" Text="Gotowy." Margin="0,5,0,0" Foreground="Gray" HorizontalAlignment="Center"/>
            <TextBlock Text="© 2026 EasyResamp by Mikołaj Pałka. All rights reserved." 
                       FontSize="10" 
                       Foreground="#444" 
                       HorizontalAlignment="Center"
                       Opacity="0.7"/>
        </StackPanel>
    </Grid>
</Window>